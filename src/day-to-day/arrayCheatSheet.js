// Array methods

Array.isArray([]);
[].flat();
[].reverse();
[].flatMap(fn);
[].forEach(fn);
[].some(fn);
[].sort((a, b) => a - b);
[].every(fn);
[].fill(value, _start, _end);
[].filter(fn);
[].find(fn);
[].findIndex(fn);
[].includes(item);
[].reduce((total, value) => total + value);
[].pop(item);
[].push(item);
[].shift(item);
[].unshift(item);
[].toString(item);
[].copyWithin(item);
[].entries(item);
[].at(index);

let array2 = Array.from(array1);
let newArray = Array.of(v1, _v2, _v3, _v4, etc);

let arrSplice = arr.splice(start, deleteCount, item1, item2, itemN);

let newArray = array1.concat(array2);
let newArray = [].map(fn);

// ...more

